git clone => composer i =>php artisan key:generate =>php artisan migrate => php artisan db:seed and run all seeders

sass client :

sass --watch ./public/client/sass/style.scss ./public/client/css/style.css

php artisan storage:link
